apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment-server
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Chart.Name }}
    component: payment-server
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.paymentServer.port }}
      targetPort: {{ .Values.services.paymentServer.port }}
  selector:
    app: {{ .Chart.Name }}
    component: payment-server
